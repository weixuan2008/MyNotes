列出所有可升级的包：
pip list --outdated

生成批量升级命令（Linux/macOS）：
pip list --outdated --format=freeze | grep -v '^\-e' | cut -d = -f 1 | xargs -n1 pip install -U

Windows替代方案：将结果手动复制到requirements.txt中，再执行：
pip install -U -r requirements.txt




python -m pip install --upgrade pip
